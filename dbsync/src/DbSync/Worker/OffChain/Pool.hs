-- | Pool metadata fetching.
--
-- Handles fetching and validation of stake pool metadata JSON from
-- URLs registered in pool registration certificates. Reserved module
-- slot pending implementation.
module DbSync.Worker.OffChain.Pool
  (
  ) where
