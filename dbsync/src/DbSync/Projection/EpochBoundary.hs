-- | Epoch boundary projection.
--
-- Computes and stores epoch-level aggregates (rewards, stake
-- distribution snapshots) at epoch boundaries.
module DbSync.Projection.EpochBoundary
  ( epochBoundaryProjection
  ) where

import Cardano.Prelude

-- | The epoch boundary projection definition.
epochBoundaryProjection :: ()
epochBoundaryProjection = panic "TODO: not implemented"
