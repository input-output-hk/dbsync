-- | Cardano SMASH Server — stake pool metadata aggregation.
--
-- Stub module. Will provide HTTP API for pool metadata queries.
-- Depends on cardano-db for DB access but NOT on cardano-db-sync.
module DbSync.SMASH.Server
  ( -- * Placeholder
    placeholder
  ) where

import Cardano.Prelude

-- | Placeholder until SMASH server is implemented.
placeholder :: Text
placeholder = "cardano-smash-server: not yet implemented"
