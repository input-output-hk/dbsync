-- | Multi-asset projection.
--
-- Tracks native tokens (multi-asset minting, burning, and transfers)
-- in dedicated tables.
module DbSync.Projection.MultiAsset
  ( multiAssetProjection
  ) where

import Cardano.Prelude

-- | The multi-asset projection definition.
multiAssetProjection :: ()
multiAssetProjection = panic "TODO: not implemented"
