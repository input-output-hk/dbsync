-- | Metadata extractor.
--
-- Stores transaction metadata (auxiliary data) in both raw and
-- decoded formats.
module DbSync.Extractor.Metadata
  ( metadataExtractor
  ) where

import Cardano.Prelude

-- | The metadata extractor definition.
metadataExtractor :: ()
metadataExtractor = panic "TODO: not implemented"
