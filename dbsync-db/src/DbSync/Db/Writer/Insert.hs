-- | INSERT writer.
--
-- Writes rows using standard INSERT statements during the
-- FollowingChainTip phase. Handles batching and conflict resolution.
module DbSync.Db.Writer.Insert
  ( -- TODO: insertRows, InsertBatch
  ) where
