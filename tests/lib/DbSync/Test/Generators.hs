-- | QuickCheck generators for db-sync types.
--
-- Provides Arbitrary instances and standalone generators for
-- GenericBlock, GenericTx, config types, etc.
module DbSync.Test.Generators
  ( -- TODO: Arbitrary instances and generators
  ) where
