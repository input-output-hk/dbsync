-- | Test fixtures.
--
-- Pre-built test data: sample blocks, transactions, configs, etc.
-- Used by unit tests to avoid constructing complex values inline.
module DbSync.Test.Fixtures
  ( -- TODO: sampleBlock, sampleTx, sampleConfig
  ) where
