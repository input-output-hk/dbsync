-- | Pool metadata fetching.
--
-- Handles fetching and validation of stake pool metadata JSON
-- from URLs registered in pool registration certificates.
module DbSync.OffChain.Pool
  ( -- TODO: fetchPoolMetadata, PoolMetadataResult
  ) where
