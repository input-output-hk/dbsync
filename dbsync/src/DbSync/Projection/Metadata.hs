-- | Metadata projection.
--
-- Stores transaction metadata (auxiliary data) in both raw and
-- decoded formats.
module DbSync.Projection.Metadata
  ( metadataProjection
  ) where

import Cardano.Prelude

-- | The metadata projection definition.
metadataProjection :: ()
metadataProjection = panic "TODO: not implemented"
